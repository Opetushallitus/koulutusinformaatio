
#
# Actual value comes from ~/oph-configuration/(common.properties | generic-events.properties)
#
# for testing, see also "junit-property-override.properties"/test-context.xml since we possibly need to
# override the broker url if test/build environment has "real" property value for the activemq broker url.
activemq.brokerurl=vm://localhost

# This is the default value for event topic name
# activemq.topic.name.events=fi.vm.sade.events.eventsTopic
