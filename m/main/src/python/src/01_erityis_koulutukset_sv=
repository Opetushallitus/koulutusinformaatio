from operator import itemgetter
from data import s, get, out, dotted_getter

url = "/lo/search/*?&facetFilters=educationType_ffm:et4&facetFilters=teachingLangCode_ffm:SV"
#url = "/lo/search/lukio"

results = get(url)
if "results" in results:
	retval = sorted([get(s("/lo/special/{x[id]}")) for x in results["results"]], key=dotted_getter("name"))
else:
	retval = []
